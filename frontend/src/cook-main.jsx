import React from 'react';
import ReactDOM from 'react-dom/client';
import { CookApp } from './screens/CookDashboard';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <CookApp />
  </React.StrictMode>
);
